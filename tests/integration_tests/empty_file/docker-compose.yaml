services:
  base:
    build:
      context: .
      dockerfile: ../base.Dockerfile

  modified:
    build:
      context: .
      dockerfile: modified.Dockerfile
      additional_contexts:
         base: "service:base"
    depends_on:
      - base